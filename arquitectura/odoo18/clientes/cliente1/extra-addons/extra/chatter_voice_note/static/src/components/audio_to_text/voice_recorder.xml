<templates id="template" xml:space="preserve">
    <t t-name="chatter_voice_note.VoiceRecorder" owl="1">
        <div class="o_container p-4">

            <!-- INFORMACIÓN DE DEBUG MEJORADA -->
            <div class="alert alert-warning mb-3">
                <strong>🔧 MODO POLLING ACTIVADO</strong><br/>
                <small>El bus de Odoo no está disponible, usando polling como alternativa</small><br/>
                User ID: <t t-esc="userId"/><br/>
                Estado: <t t-esc="state.debug_info"/><br/>
                Intentos Polling: <t t-esc="state.polling_attempts"/><br/>
                Loading: <t t-esc="state.loading_response"/>
            </div>

            <!-- BOTÓN DE PRUEBA -->
            <div class="mt-4 text-center">
                <button class="btn btn-primary btn-lg" t-on-click="testManualBus" t-att-disabled="state.loading_response">
                    <t t-if="state.loading_response">
                        <i class="fa fa-spinner fa-spin"></i> 
                        Polling... (<t t-esc="state.polling_attempts"/> intentos)
                    </t>
                    <t t-else="">
                        🧪 Test con POLLING
                    </t>
                </button>
            </div>

            <!-- RESULTADOS -->
            <div class="row mt-4">
                <div class="col-sm-6 mb-3">
                    <div class="card border-primary h-100">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-primary">
                                <i class="fa fa-message"></i> Mensaje Final
                                <t t-if="state.final_message">
                                    <span class="badge bg-success ms-2">✓ Recibido</span>
                                </t>
                            </h5>
                            <p class="card-text flex-grow-1">
                                <t t-if="state.final_message">
                                    <strong class="text-success" t-esc="state.final_message"/>
                                </t>
                                <t t-else="">
                                    <span class="text-muted">(Aún no hay mensaje)</span>
                                </t>
                            </p>
                        </div>
                    </div>
                </div>
                <div class="col-sm-6 mb-3">
                    <div class="card border-success h-100">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title text-success">
                                <i class="fa fa-robot"></i> Respuesta IA
                                <t t-if="state.answer_ia">
                                    <span class="badge bg-success ms-2">✓ Recibido</span>
                                </t>
                            </h5>
                            <p class="card-text flex-grow-1">
                                <t t-if="state.answer_ia">
                                    <strong class="text-success" t-esc="state.answer_ia"/>
                                </t>
                                <t t-else="">
                                    <span class="text-muted">(Aún no hay respuesta)</span>
                                </t>
                            </p>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </t>
</templates>